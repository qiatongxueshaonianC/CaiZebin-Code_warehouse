建堆：从下往上，从右往左，向下调整

问题一：为什么建堆是向下调整而插入新值却要向上调整：
	由于建堆过程中第一个调整的节点是第一个非叶子节点，这样每次完成一次调整后可以保证每个节点都是“以其为根节点的子树”中的权值最大的节点。这样已经完成建堆的那部分节点的结构相对较稳定，即使需要调整也是做小调整。
	插入新值需要向上调整？原因：插入是在建堆完成的基础之上，即在插入新值之前整个树结构是一个标准的“堆结构”，当插入新值时只需要和父节点做比较即可，而如果父节点没新节点大而被调整下来父亲节点不需要再继续往下调整